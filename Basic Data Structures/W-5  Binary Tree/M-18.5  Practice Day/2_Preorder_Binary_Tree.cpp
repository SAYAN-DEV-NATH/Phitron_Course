/*
void function(TreeNode<int> *root, vector<int> &v)
{
    if (root == NULL)
        return;
    v.push_back(root->data);
    function(root->left, v);
    function(root->right, v);
}
vector<int> preOrder(TreeNode<int> *root)
{
    vector<int> ans;
    function(root, ans);
    return ans;
}
*/